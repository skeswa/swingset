# Exact remaining phase-one intake: coordinator review draft

No execution occurred. This helper is outside the repository and outside every
runtime pin. It is not a recorded owner approval, year acceptance or deployment.
Eleven offline tests passed; root must review the helper and gate before use.

Existing evidence: research/workflow-output/v2-phase1/targets.csv records 17
pending captures, all wsdc_calendar.events. The published V2/V4 receipt confirms
that count. The 15 bounded 2024–2026 path CDX queries are complete. All 28
newsletter issues were acquired; eight exact reviewed policy-only bodies have
empty dispositions. Four 2016 map bodies remain undated findings, not unfetched
copies. Repeating discovery or newsletter acquisition is unnecessary.

The driver keeps the full catalog and ledger. It refuses anything except the
reviewed remaining subset of the original 17 IDs, and refuses stale successful parser
versions that would otherwise consume max_targets before the queued captures.
It skips existing finding targets, uses max_targets equal to the reviewed remaining count and never invokes
--retry-failures. Current failed rows stay explicit for review.

Gate fields, populated from real evidence by the coordinator:

- format: phase1-17-resume-v1
- coordinator_reviewed: true (engineering scope review, not owner sign-off)
- state: actual live state directory, normally /var/lib/swingset
- source: actual reviewed deployed /nix/store/...-source
- source_receipt: relative source inventory receipt filename
- source_receipt_sha256: exact actual receipt SHA256
- driver_sha256: hash of the exact copied helper, checked before any operation
- remaining_target_ids: exact pending subset of the original 17 IDs
- original_targets: all 17 immutable source/parser/URL/timestamp identities
- target_statuses: reviewed current status for every original target, including completed ones
- schema: actual deployed schema, equal to imported runtime (no migration)
- inputs: accepted_inputs row fingerprint and accepted_meta rows (including the actual input_bundle_hash pointer), in the exact
  shape returned by inputs(conn) in the helper; derives from current authority,
  and must never be generated by accepting a new input bundle
- baseline_path: resolved currently verified published candidate path
- published_sha256 and manifest_sha256: actual corresponding files
- catalog_sha256 and ledger_sha256: full current phase1 catalog/ledger
- utc_day: 2026-09-14 or a later coordinator-selected UTC execution date
- wall_seconds: at most 600
- max_additional_requests: at least max(1, remaining count) and at most 48, including actual robots/redirect/retry debits

The helper verifies the entire recorded source inventory and imported DB module
path. It opens the ordinary state lock and a raw existing connection, not the
migrating opener. Preflight opens mode=ro. It never accepts inputs, creates an
activation, signs off a year, changes controls, invokes project/link/build or
publishes. The scheduler hold file stays untouched. H13 pauses, robots,
one-request-per-host etiquette, cooldowns, H7 interpretation and actual shared
host budgets remain authoritative. Extra request gating runs inside existing
request admission via Gate.acquire, not only before polite waits.

The Archive daily budget is the smaller of the configured budget, 200, and the
current daily debit plus the reviewed additional cap. Archive spacing remains
at least ten seconds. A new UTC day is required: the September 13 ledger already
has 200 debits. The next window begins 2026-09-14 00:00 UTC (September 13 18:00
America/Denver). Preflight refuses the wrong date or a window close to rollover.
No other origin host is permitted. All seventeen locators use archived calendar
HTML, so no source fixture exception or historical scoring request is involved.

Use the actual deployed Python/runtime after the current H16 release is settled:

    PYTHONPATH=ACTUAL_PIN/src /var/lib/swingset/venv/bin/python /var/lib/swingset/operations/phase1/resume_phase1.py --gate ACTUAL_GATE --gate-sha256 ACTUAL_HASH --output NEW_PREFLIGHT_JSON

Execution adds --execute and uses a different output path. If the reviewed remaining set is empty, even --execute performs no intake and writes only the initial receipt; no completion receipt is created. With remaining work, completion is written in the intake finally block, including when intake raises. For a nonempty remainder, the request cap is remaining_count through 48; the empty-set input requires at least one as a syntactic cap but charges no requests. The coordinator
should put the command in a bounded service with a hard timeout (for example
660 seconds for a 600-second cooperative intake). HTTPX operations can overrun
an in-process cooperative deadline; the wrapper does not claim hard cancellation.
Outputs must be new files in the real state/operations tree; arbitrary external paths are rejected. Initial receipt is exclusive/no-follow and precedes requests. Completion is a
separate exclusive file preserving the original receipt. Receipts name target
statuses, current host debits, charged attempt delta, wall bound and timestamps.
A failed/interrupted run needs a fresh reviewed continuation gate using its
actual catalog/ledger; it must not reset budgets or overwrite prior receipts.

After intake, use ordinary captured-input derivation, then generate the existing
review_pack/alias_proposals outputs in a new directory. No additional acquisition
is required to prepare those packets. This does not accept any year. An owner
must resolve applicable alias and listing findings before accept_year, and
phase-two exact page-kind and year gates still apply. The original 569-group
alias packet already exists; do not duplicate it before source inventory changes.

The integration tests invoke the actual scoped-client factory and run_intake
against disposable SQLite with MockTransport and FakeClock. Robots plus an HTTP
500 retry consume all three configured debits; the first captured HTML is parsed
and the second target stays pending. A source pause persisted during the polite
wait prevents the first capture request after robots, with one charged debit
and no snapshot. Tests assert each extra gate check occurs inside SQLite's
request admission transaction, empty in-flight claims after completion, and
unchanged existing admission policies, accepted inputs and year acceptances.
They do not invoke main(), assert a real deployed pin or fabricate production
publication evidence.
