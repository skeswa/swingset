# Defaults: design/fetching.md, Politeness rules. Each block cites its owner.
# docs/sources/wsdc-calendar.md §6, lines 48–51.
[hosts."worldsdc.com"]
min_gap_seconds = 5
daily_request_budget = 10
index_interval = "24h"

# docs/sources/wsdc-registry.md §6, lines 36–38; 2s applies only to sweep.
[hosts."points.worldsdc.com"]
min_gap_seconds = 2
daily_request_budget = 1500
sweep_daily_request_budget = 20000

# docs/sources/eepro.md §6, lines 77–85.
[hosts."eepro.com"]
min_gap_seconds = 5
daily_request_budget = 600
index_interval_weekend = "1h"
index_interval_weekday = "6h"
live_interval = "15m"
cooling_interval = "6h"
round_live_interval = "12h"
round_cooling_interval = "24h"

# docs/sources/scoring-dance.md §6, lines 77–86.
[hosts."scoring.dance"]
min_gap_seconds = 5
daily_request_budget = 800
index_interval_weekend = "1h"
index_interval_weekday = "6h"
live_interval = "15m"
cooling_interval = "6h"
round_live_interval = "4h"
round_cooling_interval = "24h"
challenge_pause = "24h"

# docs/sources/world-dance-registry.md §6, lines 75–79.
[hosts."scores.worlddanceregistry.com"]
min_gap_seconds = 5
daily_request_budget = 400
live_interval = "15m"
cooling_interval = "6h"

# docs/sources/danceconvention.md §6, lines 70–77; adapter deferred to v1.1.
[hosts."danceconvention.net"]
min_gap_seconds = 10
daily_request_budget = 200
daily_byte_budget = "300MB"
index_interval_weekend = "6h"
index_interval_weekday = "24h"
live_interval = "30m"
cooling_interval = "12h"

# design/fetching.md, Archive: reserved Wayback transport, initially 10s.
[hosts."web.archive.org"]
min_gap_seconds = 10
daily_request_budget = 200
