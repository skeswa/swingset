# Read by oxfmt (which honors .prettierignore natively).
# Machine-written or captured from third-party sites — never reformat.
flake.lock
uv.lock
# Captured HTTP bodies and their metadata; byte-exact copies of source pages.
**/fixtures/
# Retained journal evidence: captures, receipts, manifests, and workflow output.
journal/evidence/*/*
!journal/evidence/*/README.md
journal/evidence/paths.json

# Generated enum reference; refresh with swingset enums --write.
docs/reference/enums.md
